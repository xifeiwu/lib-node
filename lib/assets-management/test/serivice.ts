import path from 'path';

export const SOURCE_DIR = path.join(__dirname, '.tmp/source');
export const TARGET_DIR = path.join(__dirname, '.tmp/target');
export const BACKUP_DELETED_DIR = path.join(__dirname, '.tmp/backup-deleted');
