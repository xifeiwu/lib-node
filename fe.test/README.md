## Intro

In order to avoid dependency of `@types/node`, and other node modules, unit test of functions in `lib/js` are placed in this dir.
The name and location of unit test file is the same as it's corresponding file in `lib/js` except file `service-test.ts`