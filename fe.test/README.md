## Intro

In order to avoid dependency of `@types/node`, and other node modules, unit test of functions in `lib/fe` are placed in this dir.
The name and location of unit test file is the same as it's corresponding file in `lib/fe` except file `service-test.ts`